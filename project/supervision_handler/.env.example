# PostgreSQL / TimescaleDB
DB_HOST=localhost
DB_PORT=5432
DB_NAME=plc
DB_USER=postgres
DB_PASSWORD=postgres

# Auth (demo)
JWT_SECRET=change_me_very_long_random
JWT_ISSUER=plc-mes-api
JWT_EXPIRES_MIN=120

# Simple login (demo)
API_USER=admin
API_PASSWORD=admin123

# Polling for live updates (ms)
LIVE_POLL_MS=500
